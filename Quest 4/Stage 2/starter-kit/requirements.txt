# Only needed for the interactive API documentation (api_docs/app.py).
#
# mock_services.py and multi_agent_tools.py both run on the Python standard
# library — you can import them and build your crew without installing anything
# from this file. send_slack_alert() uses urllib, not requests.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7

# Your crew framework is your choice. Uncomment whichever you go with, or keep it
# in a separate requirements file of your own.
#
# crewai>=0.60
# langgraph>=0.2
# langchain-openai>=0.2
# pyautogen>=0.3
# pydantic-ai>=0.0.14
# openai>=1.40
# anthropic>=0.34
